# Switch to Spring Boot backend
# Set this to 'true' to use Spring Boot endpoints instead of Node.js
# Both backends run on the same port (5000) with identical API endpoints
VITE_USE_SPRING_BOOT=true

# Alternative: Specify exact backend URL (optional - overrides above)
# VITE_API_BASE_URL=http://localhost:8080